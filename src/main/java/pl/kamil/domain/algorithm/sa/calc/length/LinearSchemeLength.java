package pl.kamil.domain.algorithm.sa.calc.length;

public class LinearSchemeLength implements CalculateLength {
    @Override
    public double calculateControl(double t1, double TN, int n) {
        return 0;
    }
}
